---
layout: default
title: Page Not Found
permalink: /404.html
nav_exclude: true
---

# 🔍 Redirecting...

<p id="msg">Searching for requested page...</p>

<script>
  (function () {
    var rawPath = window.location.pathname;
    var hash = window.location.hash || '';
    var search = window.location.search || '';

    // Normalize path to lowercase
    var pathLower = rawPath.toLowerCase();

    // Map of known documentation pages
    var validPages = [
      "index", "installation", "configuration", "features",
      "services", "events", "automations", "buttons",
      "quoting", "missed_messages", "autoresponder", "moderation",
      "webhooks", "rocketchat", "telegram", "tips",
      "troubleshooting", "limitations", "i18n", "support",
      "beta", "api", "architecture", "development"
    ];

    // Check if the current path matches any of our known pages (regardless of casing or subfolder)
    for (var i = 0; i < validPages.length; i++) {
      var page = validPages[i];
      // Matches /events.html, /EVENTS.html, /ha-whatsapp/EVENTS.html, /EVENTS, etc.
      var regex = new RegExp('/' + page + '(\\.html)?$', 'i');
      if (regex.test(rawPath)) {
        var target = '/ha-whatsapp/' + page + '.html' + search + hash;
        if (window.location.pathname + search + hash !== target) {
          window.location.replace(target);
          return;
        }
      }
    }

    document.getElementById('msg').innerHTML = 'The requested page could not be found. <a href="/ha-whatsapp/">Go back to home</a>';
  })();
</script>
